pipeline {
    agent none
    stages {
        stage('Build') {
         
            steps {
                sh "npm install"
            }
        }
        stage('Dev') {
            steps {
                sh "npm start"
            }
        }
    }
}